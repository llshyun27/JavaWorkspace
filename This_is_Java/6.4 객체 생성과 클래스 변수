클래스를 선언하면 ---> 객체를 생성할 설계도가 만들어진 것이다.
클래스로부터 객체를 생성하는 방법은 -> new 연산자 사용!!
new 클래스();   //new는 클래스로부터 객체를 생성시키는 연산자
클래스 변수 = new 클래스();

ex)
public class Student{     //클래스 선언
}

Student s1 = new Student();  //s1 변수가 Student 객체를 참고해 새로운 Student객체 생성.
Student s2 = new Student();

1. Student 클래스는 하나지만 new 연산자를 사용한 만큼, 객체가 메모리에 생성
2. 이러한 객체들은 Student 클래스의 인스턴스이다.
3. 같은 클래스로부터 생성되었지만 각각의 Student 객체는 자신만의 고유 데이터를 가지며 메모리에서 활동.

